import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      alignItems: 'center',
      padding: 16,
      borderWidth: 1,
      borderRadius: 4,
      borderColor: '#E2E8F0',
    },
    input: {
      width: '90%',
      borderColor: '#E2E8F0',
  },
    eyeIconContainer: {
      width: '10%',
      justifyContent: 'center',
      alignItems: 'center',
    },
  });